# 飞书登录流程

Apex 支持两种访问场景，共用同一套后端鉴权体系（JWT）。

---

## 场景一：飞书客户端内（H5 静默登录）

用户从飞书工作台或消息卡片点击进入，飞书 JSSDK 静默获取临时 code，POST 给后端换 JWT，全程用户无感知。

### 前置配置（飞书开放平台）

| 配置项 | 值 |
|--------|-----|
| 应用类型 | 企业自建应用 |
| 功能 | 网页应用（H5） |
| 网页 URL（入口） | `https://your-domain.com/api/auth/feishu/launch` |
| 可信域名 | `your-domain.com` |
| 所需权限 | `contact:user.base:readonly` |

### 流程图

```
用户点击飞书工作台应用
        │
        ▼
飞书 WebView 加载入口
GET /api/auth/feishu/launch  →  302  →  /matrix
        │
        ▼
FeishuGuard 检测到飞书环境（UA 含 Lark / window.h5sdk 存在）→ 透传
        │
        ▼
RequireAuth 检查 localStorage token
  ├─ 有效 token → GET /api/auth/me → 渲染应用
  └─ 无 token  → 调用飞书 JSSDK
                  window.tt.requestAccess({ appID })
                  → 静默获得临时 code（5 分钟有效）
                         │
                         ▼
                POST /api/auth/feishu/code2token { code }
                         │
                         ▼
                后端换取用户信息 → 签发 JWT
                         │
                         ▼
                前端存 token → 渲染应用
```

---

## 场景二：外部浏览器访问

用户在普通浏览器中打开应用链接，`FeishuGuard` 检测到非飞书环境，展示引导页并提供「在飞书中打开」按钮，点击后通过 applink 拉起飞书客户端并直接进入应用。

```
外部浏览器访问 https://your-domain.com
        │
        ▼
FeishuGuard 检测到非飞书环境
        │
        ▼
展示引导页：「请在飞书中打开」
        │ 用户点击按钮
        ▼
https://applink.feishu.cn/client/web_app/open?appId=cli_xxx
        │
        ├─ 已安装飞书 → 拉起飞书客户端，进入应用（走场景一流程）
        └─ 未安装飞书 → 跳转飞书下载页
```

> 外部浏览器**不支持**直接登录使用，必须通过飞书客户端访问。

---

## 本地开发 Mock 流程

`import.meta.env.DEV === true` 时，`FeishuGuard` 豁免检测，`RequireAuth` 走 mock 登录：

```
本地 vite dev 访问
        │
        ▼
FeishuGuard DEV 豁免 → 透传
        │
        ▼
RequireAuth 检测到非飞书环境
        │
        ▼
POST /api/auth/mock-login
        │
        ▼
后端签发包含 mock 用户信息的真实 JWT
        │
        ▼
前端存 token → GET /api/auth/me → 渲染应用
```

---

## Token 失效处理

```
前端请求收到 401
        │
        ▼
清除 localStorage access_token
        │
        ▼
重走飞书 JSSDK 静默登录（用户无感知）
```

---

## 后端接口一览

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| POST | `/api/auth/feishu/code2token` | 飞书客户端 JSSDK code 换 JWT | 无 |
| GET  | `/api/auth/feishu/launch` | 飞书工作台入口，302 到前端 /matrix | 无 |
| GET  | `/api/auth/me` | 获取当前登录用户信息 | JWT |
| POST | `/api/auth/mock-login` | Mock 登录（仅本地开发） | 无 |

---

## 环境变量

```bash
# 后端 .env.production
FEISHU_APP_ID=cli_xxxxxxxxxxxxxxxx
FEISHU_APP_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
FRONTEND_URL=https://your-domain.com
JWT_SECRET=your-random-secret-string
JWT_EXPIRE_MINUTES=1440   # 24 小时

# 前端 .env / .env.production
VITE_FEISHU_APP_ID=cli_xxxxxxxxxxxxxxxx
VITE_API_BASE_URL=https://your-domain.com
```

---

## 注意事项

- JSSDK code **一次性使用**，5 分钟内有效，用完即失效
- JWT 有效期 24 小时，过期后由 RequireAuth 自动静默刷新
- 飞书 JSSDK（`window.h5sdk` / `window.tt`）只在飞书 WebView 中有效
- 生产环境必须 HTTPS，可信域名需与实际部署域名完全一致
- `mock-login` 接口无鉴权，**仅限本地开发**，生产环境不要调用
